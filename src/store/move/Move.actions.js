import { moveSlice } from "./Move.reducer";

export const {
    addMoves, 
    addMove, 
    removeMoves,
    removeMove, 
    updateMoves, 
    updateMove,
    updateMovePosition
} = moveSlice.actions

