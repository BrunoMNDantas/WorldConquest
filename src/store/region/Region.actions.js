import { regionSlice } from "./Region.reducer";

export const {
    addRegions, 
    addRegion, 
    removeRegions,
    removeRegion, 
    updateRegions, 
    updateRegion,
    updateRegionUnitsAmmount,
    updateRegionPlayerId
} = regionSlice.actions

